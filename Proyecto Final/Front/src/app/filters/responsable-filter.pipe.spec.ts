import { ResponsableFilterPipe } from './responsable-filter.pipe';

describe('ResponsableFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ResponsableFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
