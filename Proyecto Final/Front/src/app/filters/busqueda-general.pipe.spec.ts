import { BusquedaGeneralPipe } from './busqueda-general.pipe';

describe('BusquedaGeneralPipe', () => {
  it('create an instance', () => {
    const pipe = new BusquedaGeneralPipe();
    expect(pipe).toBeTruthy();
  });
});
