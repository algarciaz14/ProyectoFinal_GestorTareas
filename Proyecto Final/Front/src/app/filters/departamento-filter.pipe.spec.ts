import { DepartamentoFilterPipe } from './departamento-filter.pipe';

describe('DepartamentoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DepartamentoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
