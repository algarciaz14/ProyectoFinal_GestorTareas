import { TareaFilterPipe } from './tarea-filter.pipe';

describe('TareaFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TareaFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
